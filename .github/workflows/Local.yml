---
name: Test workflow
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v3
      - run: ./decrypt.sh
        env:
          SECRET_PASSPHRASE: ${{ secrets.SECRET_PASSPHRASE }}
      - run: ./tfrun-in-docker.sh init
